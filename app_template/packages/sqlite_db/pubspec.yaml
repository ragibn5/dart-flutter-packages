name: sqlite_db
description: SQLite database service with abstract contracts and sqflite implementation.

publish_to: none

environment:
  sdk: ^3.0.6
  flutter: ^3.10.0

dependencies:
  equatable: ^2.0.8
  path: ^1.9.1
  sqflite: ^2.4.2
  disposable:
    path: ../disposable
  initializable:
    path: ../initializable
  functions:
    path: ../../../functions

dev_dependencies:
  test: ^1.25.5
  mocktail: ^1.0.5
  sqflite_common_ffi: ^2.4.0+2
