name: sqlite_db
description: SQLite database.

publish_to: none

environment:
  sdk: ^3.0.6

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  sqflite: ^2.4.2
  disposable:
    path: ../../../disposable
  initializable:
    path: ../initializable
  functions:
    path: ../../../functions

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
