name: shared_models
description: A collection of common shared models for the host app.

publish_to: none

environment:
  sdk: ^3.0.6

dependencies:
  equatable: ^2.0.8

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0
