name: json_parser_analyzer
description: Analysis server plugin to enforce json_parser compatible structures.

repository: https://github.com/Ragibn5/dart-flutter-packages
issue_tracker: https://github.com/Ragibn5/dart-flutter-packages/issues
homepage: https://github.com/Ragibn5/dart-flutter-packages/tree/main/json_parser/json_parser_analyzer

# This prevents you from accidentally publish to pub.dev
publish_to: 'none'

# Library version
version: 0.0.1

# Range of compatible SDK versions
environment:
  sdk: ^3.10.9

# Regular dependencies
# (Bundled in executable artifact)
dependencies:
  path: ^1.9.0
  yaml: ^3.1.3
  json_parser_annotations:
    path: ../json_parser_annotations
  string_extensions:
    path: ../../string_extensions
  functions:
    path: ../../functions
  analysis_server_core:
    path: ../../analysis_server_core

# Dev dependencies
# (NOT Bundled in executable artifact - For DEV use only)
dev_dependencies:
  test: ^1.25.6
  mocktail: ^1.0.4
  lints: ^6.0.0
  very_good_analysis: ^7.0.0
  analysis_plugin_test_helper:
    path: ../../analysis_plugin_test_helper
