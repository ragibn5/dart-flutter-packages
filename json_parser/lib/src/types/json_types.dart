/// The base JSON type representing any valid JSON value.
typedef Json = Object?;

/// The type of a JSON array.
typedef JsonList = List<Json>;

/// The type of a JSON object.
typedef JsonMap = Map<String, Json>;
