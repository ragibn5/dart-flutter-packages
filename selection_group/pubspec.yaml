name: selection_group
description: A fully customizable selection group.

repository: https://github.com/Ragibn5/dart-flutter-packages
issue_tracker: https://github.com/Ragibn5/dart-flutter-packages/issues
homepage: https://github.com/Ragibn5/dart-flutter-packages/tree/main/selection_group

# This prevents you from accidentally publish to pub.dev
publish_to: 'none'

# Library version
version: 0.0.1

# Range of compatible SDK versions
environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  # SDK
  flutter:
    sdk: flutter
  mocktail: ^1.0.4

dev_dependencies:
  # Test
  flutter_test:
    sdk: flutter

  # Lints
  flutter_lints: ^3.0.1
  very_good_analysis: ^5.1.0