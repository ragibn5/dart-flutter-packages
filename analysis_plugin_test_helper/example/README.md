# Example

This package is specifically designed to help write tests, so the best
example is the tests themselves.

See the
[`test`](https://github.com/Ragibn5/dart-flutter-packages/tree/main/analysis_plugin_test_helper/test)
folder for complete, real-world demonstrations of every parser and the
`DartUnitResolver`.
