name: json_parser_generator
description: Generator counterpart of the `json_coder` package.

repository: https://github.com/Ragibn5/dart-flutter-packages
issue_tracker: https://github.com/Ragibn5/dart-flutter-packages/issues
homepage: https://github.com/Ragibn5/dart-flutter-packages/tree/main/json_coder/json_parser_generator

# Library version
version: 1.0.0

# Range of compatible SDK versions
environment:
  sdk: ^3.11.0

# Regular dependencies
# (Bundled in executable artifact)
dependencies:
  path: ^1.9.0
  meta: ^1.12.0
  glob: ^2.1.2
  analyzer: ^8.0.0
  build_config: ^1.3.0
  code_builder: ^4.11.1
  dart_style: ^3.1.2
  dart_functionals: ^1.0.1
  json_parser_annotations: ^1.0.1
  generator_core: ^1.0.1

# Dev dependencies
# (NOT Bundled in executable artifact - For DEV use only)
dev_dependencies:
  test: ^1.25.6
  mocktail: ^1.0.4
  lints: ^6.0.0
  very_good_analysis: ^7.0.0
  package_config: ^2.2.0
